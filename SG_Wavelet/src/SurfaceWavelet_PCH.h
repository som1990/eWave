#pragma once
#include <array>
#include <iostream>
#include <vector>
#include "math/Math.h"

using Vec2 = std::array<float, 2>;
using Vec3 = std::array<float, 3>;
using Vec4 = std::array<float, 4>;

enum dimOption { X = 0, Y, THETA, K };